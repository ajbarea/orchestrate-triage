[project]
name = "orchestrate-triage-docs"
version = "0.1.0"
description = "Zensical documentation build for the orchestrate-triage hackathon agent."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.24",
]

[tool.uv]
package = false
